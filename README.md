# NodeJS Remote Control Websockets 

## Installation
`npm i`

## Run
**Dev Env**
`npm run start:dev`

* App served @ `http://localhost:8181` with nodemon

**Production**

`npm run start`

**commands**

`npm run start:dev` 
`npm run start`
